version: '3.4'

services:
  logsapi:
    image: ${DOCKER_REGISTRY-}logsapi
    container_name: logs_api
    build:
      context: .
      dockerfile: LogsAPI/Dockerfile
    ports:
        - "8080:80"
        - "8443:443"
        - "8022:22"